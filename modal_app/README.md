# Modal BindCraft app (skeleton)

Status: **not deployed**. Poller fail-closes on `/confirm` binder until:

- GPU image with BindCraft + AF2
- Volume `bindcraft-weights` with params
- `modal deploy` and `MODAL_BINDCRAFT_APP` set on the poller

See `bindcraft_app.py` and `/workspace/FEATURE-modal-bindcraft-compute.md`.
